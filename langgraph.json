{
  "name": "multiagent-memory-demo",
  "version": "1.0.0",
  "description": "LangGraph project demonstrating STM + LTM memory with controller routing",
  "dependencies": "requirements.txt",
  "env": {
    "OPENAI_API_KEY": "@env.OPENAI_API_KEY"
  },
  "graphs": {
    "memory_demo": {
      "entrypoint": "graph:app",
      "expose": true
    }
  }
}
